#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int xyz(int (*q)[])
{
    printf("Namaste Duniya!!!\n");
}

int abc(int **x)
{
}

int main(int argc, char **argv)
{
    // static int s;
    // printf("size = %ld\n", sizeof(s));

    // int(*p)[5];
    // int **q;
    // xyz(p);

    // int *a[5];
    // abc(a);

    int temp;
    char *temp;
    return 0;
}